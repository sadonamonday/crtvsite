/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 doesn't require content globs.
  theme: {
    extend: {},
  },
  // No plugins needed here for Vite integration; it's configured in vite.config.js
}
